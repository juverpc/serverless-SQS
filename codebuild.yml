version: 0.1

phases:

  install:

    commands:
      - npm install
      - npm install -g serverless
      - npm install serverless-deployment-bucket --save-dev
     

  build:

    commands:
      - serverless deploy 
   
